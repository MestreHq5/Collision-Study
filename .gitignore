__pycache__/
*.pyc

# PyInstaller build output
build/
dist/

# Local environment overrides (see .env.example)
.env

# Per-run workspace artifacts (Desktop/Collision_Study/<group>/...) never
# live in the repo -- nothing here to ignore for those, they're outside the
# working tree by construction (see helper.file_manager).
